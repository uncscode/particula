# Documentation Updates

- Update `docs/Features/Roadmap/data-oriented-gpu.md` with the shipped active,
  free, invalid, ordering, diagnostics, and capacity contracts.
- Update the relevant `docs/Features/` GPU/container guide with CPU and direct
  Warp APIs, request/sidecar shape tables, ownership, and explicit transfers.
- Update `AGENTS.md` with fixed-slot invariants and focused CPU/Warp test
  commands for downstream feature implementers.
- Update `.opencode/guides/testing_guide.md` if exact diagnostic, identity, and
  pre-mutation snapshot patterns need reusable guidance.
- Cross-link E6-F6 exhaustion and E6-F7/E6-F8 nucleation documentation; make
  clear that this feature does not select an exhaustion policy.
- Mark E6-F5 plan phases and change log as shipped when implementation lands.

No user example is required until a physical particle-source process consumes
activation; E6-F9 owns the integrated direct-step example.
