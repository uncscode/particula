# Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-07-21 | Initial E6-F3/T3 feature plan drafted with seven issue-sized phases for neutral spherical/rectangular GPU wall loss, deterministic CPU coefficient parity, statistical survival validation, persistent caller-owned RNG, fixed-slot clearing, and explicit Epic G/E6 sibling boundaries | plan-feature-drafter |
| 2026-07-22 | Recorded shipped E6-F3-P1 / #1401: canonical neutral fp64 transport ownership moved to `particula.gpu.properties`, legacy dynamics definitions/re-exports removed, consumers migrated, defined slip domain behavior added, and device-only Debye/`x_coth_x` primitives validated. Recorded that wall-loss assembly/API, charged physics, and CPU changes remain deferred. | plan-update-full |
| 2026-07-23 | Recorded shipped E6-F3-P2 / #1402: added concrete internal fp64 Warp spherical and rectangular neutral Crump-Seinfeld coefficient helpers in `particula/gpu/dynamics/wall_loss_funcs.py`, plus guarded CPU/Warp parity and smoke coverage in `particula/gpu/dynamics/tests/wall_loss_funcs_test.py`. No public export, CPU physics, validation/configuration, charged physics, container mutation, or RNG behavior changed. | plan-update-full |
| 2026-07-23 | Recorded shipped E6-F3-P3 / #1403: added frozen concrete-only `NeutralWallLossConfig`, write-free `wall_loss_step_gpu` preflight, lazy kernel export, and Warp-guarded atomicity contract tests. Added bounded P3 support/deferred-contract guidance to `docs/index.md` and `AGENTS.md`. Coefficient/removal execution and RNG lifecycle remain deferred. | plan-update-full |
| 2026-07-22 | Recorded shipped E6-F3-P4 / #1404: `wall_loss_step_gpu` now executes bounded neutral particle-resolved removal after frozen P3 preflight. Positive-time calls normalize environments, evaluate spherical/rectangular coefficients for usable slots, use deterministic local seed/slot draws, and clear removed masses, concentration, and charge. Zero time is write-free after preflight and supplied `rng_states` is neither initialized nor advanced. Focused geometry, mask, sparse-layout, stochastic, and pre-launch-atomicity coverage plus P4 contract documentation were added. | plan-update-full |
| 2026-07-23 | Recorded shipped E6-F3-P5 / #1405: `wall_loss_step_gpu` now gives supplied `(n_boxes,)` `wp.uint32` Warp RNG sidecars persistent caller-owned lifecycle. Positive-time execution advances each box sequentially for eligible slots only; omitted state is private and per-call, and supplied state resets only with `initialize_rng=True`. Zero-time and rejected calls preserve supplied sidecars exactly. Lifecycle, eligible-only/no-draw, rejection, and opt-in benchmark-smoke coverage shipped with updated user and contract documentation. | plan-update-full |
| 2026-07-23 | Recorded shipped E6-F3-P6 / #1406: added test-only `particula/gpu/kernels/tests/wall_loss_parity_test.py` with an independent CPU-equation coefficient/eligibility diagnostic, 100-seed fresh-state and persistent-sidecar 3-sigma survival checks, exact no-op checks, and lazy import-boundary smoke coverage. Warp CPU is the baseline and CUDA is additive. No production physics or public export changed. | plan-update-full |
