<!-- TEMPLATE: Replace this entire file with communication plan -->

Document who receives updates, how often metrics are reviewed, and where to
log status.

**Example (M23):**
- Owner updates go to `@gorkowski` with phase-level progress summaries.
- Review wrapper-facing contract changes in PR descriptions with explicit
  before/after examples for diagnostics and status outputs.
- Record newly discovered wrapper gaps through `feedback_log` write mode.
- Summarize shipped changes in dev-plan indexes/README after final docs phase.
- Escalate remaining gaps that still require generic shell access into a
  follow-up plan instead of expanding this plan ad hoc.
