<!-- TEMPLATE: Replace this entire file with your documentation plan -->

List every document that must change when this feature ships.

**Common documents to update:**
- `README.md` -- if the feature changes CLI commands or quick start
- `AGENTS.md` -- if new agents or workflows are added
- `.opencode/guides/` guides -- architecture, testing, code style, etc.
- `docs/Examples/` -- if new usage patterns are introduced
- `.opencode/plans/sections/` -- plan section files when plan content changes

**Example (E16-F6):**
- Update `docs/Examples/operations/auto-mode-runbook.md` with final handoff steps
- Update `AGENTS.md` with `shipper-auto-final` agent description
- Update `docs/Examples/setup/workflow-reference.md` with `ship-auto-final` workflow
- Update `.opencode/plans/sections/` content with shipped status when needed
