{
  "name": "generate",
  "version": "1.0.0",
  "description": "Generate workflow: interpret issue and create new issues",
  "workflow_type": "generate",
  "steps": [
    {
      "type": "agent",
      "name": "Interpret Issue",
      "agent": "issue-generator",
      "prompt": "${issue_number} --adw-id ${adw_id}",
      "model": "heavy",
      "timeout": 1800,
      "retry": {
        "max_retries": 1,
        "initial_delay": 5.0,
        "backoff": 2.0
      }
    }
  ]
}
