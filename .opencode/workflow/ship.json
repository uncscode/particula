{
  "name": "ship",
  "version": "2.0.0",
  "description": "Ship workflow - Finalize and ship implementation changes",
  "description_long": "Finalizes implementation by running linting, creating commits, pushing branches, and creating pull requests",
  "workflow_type": "complete",
  "steps": [
    {
      "type": "agent",
      "name": "Ship Implementation",
      "agent": "shipper",
      "prompt": "$ARGUMENTS --adw-id ${adw_id}",
      "model": "base",
      "timeout": 600,
      "retry": {
        "max_retries": 2,
        "initial_delay": 5.0,
        "backoff": 2.0
      }
    }
  ]
}
